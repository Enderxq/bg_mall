<!--
<p id="notice"><%= notice %></p>

<h1>Addresses</h1>

<table>
  <thead>
    <tr>
      <th>Addr detail</th>
      <th>Person</th>
      <th>Phone</th>
      <th>Post code</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @addresses.each do |address| %>
      <tr>
        <td><%= address.addr_detail %></td>
        <td><%= address.person %></td>
        <td><%= address.phone %></td>
        <td><%= address.post_code %></td>
        <td><%= link_to 'Show', address %></td>
        <td><%= link_to 'Edit', edit_address_path(address) %></td>
        <td><%= link_to 'Destroy', address, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Address', new_address_path %>
-->
<style>
label {
  font-size: 14px;
  font-weight: normal;
}
a {
  color: #337ab7;
  text-decoration: none;
}
a:hover, a:focus {
  color: #23527c;
  text-decoration: underline;
  background-color: transparent
}
a:focus {
  outline: 5px auto -webkit-focus-ring-color;
  outline-offset: -2px;
}

</style>

<h1>我的收货地址</h1>
<div class="row">
  <div class="container">
    <div>
      <div class="pull-right">
        <a class="btn btn-info btn-xs new-address-btn" href="/addresses/new"> 添加收货地址</a>
      </div>
      <div class="clearfix"></div>
      <br>
    <% @addresses.all.each do |address| %>
      <ul class="list-group">
        <li class="list-group-item">
      <label>
         <input type="radio" name="address_id" value="1" checked="">
           <%=address.person %> 
           <%=address.addr_detail%> 
           <%=address.phone %>
         <%= link_to '编辑', edit_address_path(address) %>
         <a data-confirm="确认删除该地址吗？" class="remove-address-btn inline" data-remote="true" rel="nofollow" data-method="delete" href="/addresses/1">删除</a>
       </label>

    <% end %>
</li>
      </ul>
    </div> 
  </div>
</div>


